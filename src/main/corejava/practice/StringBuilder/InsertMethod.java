package practice.StringBuilder;

public class InsertMethod {
    public static void main(String[] args) {

      StringBuffer sb = new StringBuffer("Memory");
        System.out.println(sb.length());
        sb.insert(5,"isationar");
        System.out.println(sb);// it can be convert memory to memorisationary w.t.h. of stringbuffer with insert method

    }
}
